export const metadata = {
  title: 'Settings | DraftCopyAI Portal',
  description: 'Manage your account settings and subscription.',
};

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
